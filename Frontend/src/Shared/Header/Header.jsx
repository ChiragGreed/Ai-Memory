import './Header.scss'

const Header = () => {
    return (
        <header className="app-header">
            <span className="app-header__eyebrow">Collection</span>
            <h1 className="app-header__title">Items</h1>
        </header>
    )
}

export default Header
